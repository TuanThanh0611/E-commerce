import type { ValueProvider } from '@angular/core';
import { alwaysRejected } from '../utils/functions';
export declare const CACHES_MOCK: {
    delete: () => Promise<boolean>;
    has: () => Promise<boolean>;
    keys: () => Promise<never[]>;
    match: typeof alwaysRejected;
    open: typeof alwaysRejected;
};
export declare const UNIVERSAL_CACHES: ValueProvider;
