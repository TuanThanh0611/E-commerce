"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildImageExecutor = buildImageExecutor;
const path = require("path");
const boot_utils_1 = require("../../utils/boot-utils");
async function buildImageExecutor(options, context) {
    const root = path.resolve(context.root, options.root);
    return (0, boot_utils_1.runBootPluginCommand)('build-image', options.args, {
        cwd: root,
        ignoreWrapper: options.ignoreWrapper,
        runFromParentModule: options.runFromParentModule,
    });
}
exports.default = buildImageExecutor;
//# sourceMappingURL=executor.js.map