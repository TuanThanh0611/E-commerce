"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.formatCheckExecutor = formatCheckExecutor;
const path = require("path");
const boot_utils_1 = require("../../utils/boot-utils");
async function formatCheckExecutor(options, context) {
    const root = path.resolve(context.root, options.root);
    return (0, boot_utils_1.runBootPluginCommand)('check-format', options.args, {
        cwd: root,
        ignoreWrapper: options.ignoreWrapper,
        runFromParentModule: options.runFromParentModule,
    });
}
exports.default = formatCheckExecutor;
//# sourceMappingURL=executor.js.map